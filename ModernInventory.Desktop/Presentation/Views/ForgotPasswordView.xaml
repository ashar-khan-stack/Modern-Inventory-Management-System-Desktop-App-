<UserControl x:Class="ModernInventory.Desktop.Presentation.Views.ForgotPasswordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:conv="clr-namespace:ModernInventory.Desktop.Presentation.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <UserControl.Resources>
        <conv:BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <Grid Background="#0A0F1D">
        <Border Width="440" HorizontalAlignment="Center" VerticalAlignment="Center"
                Background="#131D31" BorderBrush="#22324F" BorderThickness="1"
                CornerRadius="12" Padding="32">
            <StackPanel>
                <TextBlock Text="Reset Password" Foreground="White" FontWeight="Bold" FontSize="20" HorizontalAlignment="Center" />
                <TextBlock Text="Answer your security recovery question" Foreground="{StaticResource TextMutedBrush}" FontSize="12" HorizontalAlignment="Center" Margin="0,4,0,20" />

                <!-- Status Message -->
                <Border Background="#064E3B" BorderBrush="#059669" BorderThickness="1" CornerRadius="6" Padding="12,8" Margin="0,0,0,16"
                        Visibility="{Binding StatusMessage, Converter={StaticResource BoolToVis}}">
                    <TextBlock Text="{Binding StatusMessage}" Foreground="#A7F3D0" FontSize="12" TextWrapping="Wrap" />
                </Border>

                <!-- Email Input -->
                <TextBlock Text="Account Email *" Foreground="{StaticResource TextMutedBrush}" FontSize="11" Margin="0,0,0,4" />
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBoxStyle}" Height="36" Margin="0,0,0,12" />

                <!-- Security Answer Input -->
                <TextBlock Text="Security Answer *" Foreground="{StaticResource TextMutedBrush}" FontSize="11" Margin="0,0,0,4" />
                <TextBox Text="{Binding SecurityAnswer, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBoxStyle}" Height="36" Margin="0,0,0,12" />

                <!-- New Password Input -->
                <TextBlock Text="New Password *" Foreground="{StaticResource TextMutedBrush}" FontSize="11" Margin="0,0,0,4" />
                <TextBox Text="{Binding NewPassword, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBoxStyle}" Height="36" Margin="0,0,0,20" />

                <Button Content="Reset Password" Command="{Binding ResetPasswordCommand}" Style="{StaticResource PrimaryButtonStyle}" Height="40" Margin="0,0,0,16" />
                <Button Content="Back to Login" Command="{Binding GoToLoginCommand}" Background="Transparent" Foreground="{StaticResource TextMutedBrush}" BorderThickness="0" Cursor="Hand" FontSize="13" HorizontalAlignment="Center" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
