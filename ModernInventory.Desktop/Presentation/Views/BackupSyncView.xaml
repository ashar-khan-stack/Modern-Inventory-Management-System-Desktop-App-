<UserControl x:Class="ModernInventory.Desktop.Presentation.Views.BackupSyncView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:conv="clr-namespace:ModernInventory.Desktop.Presentation.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900">

    <UserControl.Resources>
        <conv:BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="0,0,0,32">
            <!-- Header -->
            <TextBlock Text="Local Database Backups" Foreground="White" FontSize="20" FontWeight="Bold" />
            <TextBlock Text="Manage SQLite database snapshots, integrity checks, and local mutation tracking" Foreground="{StaticResource TextMutedBrush}" FontSize="12" Margin="0,4,0,24" />

            <!-- Status Alert -->
            <Border Background="#064E3B" BorderBrush="#059669" BorderThickness="1" CornerRadius="6" Padding="14,10" Margin="0,0,0,20"
                    Visibility="{Binding StatusMessage, Converter={StaticResource BoolToVis}}">
                <TextBlock Text="{Binding StatusMessage}" Foreground="#A7F3D0" FontSize="13" />
            </Border>

            <!-- Actions Row -->
            <Grid Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Create Backup Card -->
                <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}">
                    <StackPanel>
                        <TextBlock Text="Create Local Backup" Foreground="White" FontWeight="Bold" FontSize="16" />
                        <TextBlock Text="Creates an atomic, verified copy of the SQLite database with SHA256 checksum and metadata." Foreground="{StaticResource TextMutedBrush}" FontSize="12" Margin="0,4,0,16" TextWrapping="Wrap" />

                        <Button Content="💾  Create Backup Now" Command="{Binding CreateBackupCommand}" Style="{StaticResource PrimaryButtonStyle}" Height="40" HorizontalAlignment="Left" />
                    </StackPanel>
                </Border>

                <!-- Sync Queue Status Card -->
                <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}">
                    <StackPanel>
                        <TextBlock Text="Offline Transaction Queue" Foreground="White" FontWeight="Bold" FontSize="16" />
                        <TextBlock Text="Operations queued locally during offline usage. This foundation supports future reconciliations." Foreground="{StaticResource TextMutedBrush}" FontSize="12" Margin="0,4,0,16" TextWrapping="Wrap" />

                        <StackPanel Orientation="Horizontal">
                            <Border Background="#0F172A" BorderBrush="{StaticResource BorderDarkBrush}" BorderThickness="1" CornerRadius="6" Padding="12,6" Margin="0,0,12,0">
                                <TextBlock Text="{Binding PendingQueueCount, StringFormat='Pending Operations: {0}'}" Foreground="White" FontWeight="SemiBold" FontSize="13" />
                            </Border>
                            <Button Content="↻  Refresh Status" Command="{Binding RefreshCommand}" Background="#334155" Foreground="White" Padding="12,6" BorderThickness="0" Cursor="Hand" />
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Backup History Grid -->
            <Border Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Local Backup History" Foreground="White" FontSize="15" FontWeight="Bold" Margin="0,0,0,16" />

                    <DataGrid ItemsSource="{Binding BackupHistory}" SelectedItem="{Binding SelectedBackup}" AutoGenerateColumns="False"
                              Background="Transparent" RowBackground="#131D31" AlternatingRowBackground="#1E293B"
                              BorderThickness="0" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#334155"
                              Foreground="White" HeadersVisibility="Column" IsReadOnly="True">
                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#0F172A" />
                                <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                                <Setter Property="Padding" Value="12,8" />
                                <Setter Property="BorderThickness" Value="0" />
                            </Style>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Padding" Value="12,10" />
                                <Setter Property="BorderThickness" Value="0" />
                            </Style>
                        </DataGrid.Resources>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="FILE NAME" Binding="{Binding FileName}" Width="*" />
                            <DataGridTextColumn Header="SIZE (BYTES)" Binding="{Binding FileSizeBytes, StringFormat='{}{0:N0} bytes'}" Width="140" />
                            <DataGridTextColumn Header="SHA256 CHECKSUM" Binding="{Binding Checksum}" Width="180" />
                            <DataGridTextColumn Header="CREATED AT (UTC)" Binding="{Binding CreatedAt, StringFormat='{}{0:yyyy-MM-dd HH:mm:ss}'}" Width="150" />
                            <DataGridTemplateColumn Header="ACTIONS" Width="110">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="↩ Restore" 
                                                Command="{Binding DataContext.RestoreBackupCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                CommandParameter="{Binding}"
                                                Background="#047857" Foreground="White" Padding="8,4" FontSize="11" 
                                                BorderThickness="0" Cursor="Hand" FontWeight="SemiBold" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
