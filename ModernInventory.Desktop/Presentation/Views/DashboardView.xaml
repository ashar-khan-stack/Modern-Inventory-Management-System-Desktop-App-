<UserControl x:Class="ModernInventory.Desktop.Presentation.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:conv="clr-namespace:ModernInventory.Desktop.Presentation.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="1000">

    <UserControl.Resources>
        <conv:CurrencyFormatConverter x:Key="CurrencyFormat" />
        <conv:BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="0,0,0,32">
            <!-- Header Section -->
            <Grid Margin="0,0,0,24">
                <StackPanel>
                    <TextBlock Text="Executive Financial &amp; Inventory Overview" Foreground="White" FontSize="20" FontWeight="Bold" />
                    <TextBlock Text="Authoritative local SQLite metrics calculated across sales, items, and accounting records" Foreground="{StaticResource TextMutedBrush}" FontSize="12" Margin="0,4,0,0" />
                </StackPanel>

                <Button Content="↻  Refresh Metrics" Command="{Binding LoadMetricsCommand}" HorizontalAlignment="Right" VerticalAlignment="Center"
                        Background="#1E293B" Foreground="White" BorderBrush="{StaticResource BorderDarkBrush}" BorderThickness="1"
                        Padding="14,8" Cursor="Hand" FontWeight="SemiBold" FontSize="12" />
            </Grid>

            <!-- Top Key Metrics Cards (Row 1) -->
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Today's Sales -->
                <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}">
                    <StackPanel>
                        <TextBlock Text="TODAY'S SALES" Foreground="{StaticResource TextMutedBrush}" FontSize="11" FontWeight="Bold" />
                        <TextBlock Text="{Binding Metrics.TodaySales, Converter={StaticResource CurrencyFormat}}" Foreground="White" FontSize="22" FontWeight="Bold" Margin="0,8,0,4" />
                        <TextBlock Text="Direct POS Invoices" Foreground="#10B981" FontSize="11" />
                    </StackPanel>
                </Border>

                <!-- Monthly Sales -->
                <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}">
                    <StackPanel>
                        <TextBlock Text="THIS MONTH'S REVENUE" Foreground="{StaticResource TextMutedBrush}" FontSize="11" FontWeight="Bold" />
                        <TextBlock Text="{Binding Metrics.MonthlySales, Converter={StaticResource CurrencyFormat}}" Foreground="White" FontSize="22" FontWeight="Bold" Margin="0,8,0,4" />
                        <TextBlock Text="{Binding Metrics.TotalSalesCount, StringFormat='Total Invoices: {0}'}" Foreground="{StaticResource TextMutedBrush}" FontSize="11" />
                    </StackPanel>
                </Border>

                <!-- Net Profit -->
                <Border Grid.Column="4" Style="{StaticResource CardBorderStyle}">
                    <StackPanel>
                        <TextBlock Text="NET PROFIT (M-T-D)" Foreground="{StaticResource TextMutedBrush}" FontSize="11" FontWeight="Bold" />
                        <TextBlock Text="{Binding Metrics.NetProfit, Converter={StaticResource CurrencyFormat}}" Foreground="#34D399" FontSize="22" FontWeight="Bold" Margin="0,8,0,4" />
                        <TextBlock Text="Sales - COGS - Expenses" Foreground="{StaticResource TextMutedBrush}" FontSize="11" />
                    </StackPanel>
                </Border>

                <!-- Low Stock Alert -->
                <Border Grid.Column="6" Style="{StaticResource CardBorderStyle}">
                    <StackPanel>
                        <TextBlock Text="LOW STOCK ALERTS" Foreground="{StaticResource TextMutedBrush}" FontSize="11" FontWeight="Bold" />
                        <TextBlock Text="{Binding Metrics.LowStockCount}" Foreground="#F87171" FontSize="22" FontWeight="Bold" Margin="0,8,0,4" />
                        <TextBlock Text="{Binding Metrics.TotalProductsCount, StringFormat='Catalog: {0} items'}" Foreground="{StaticResource TextMutedBrush}" FontSize="11" />
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Liquidity & Balances (Row 2) -->
            <Grid Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Receivables -->
                <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}">
                    <StackPanel>
                        <TextBlock Text="CUSTOMER RECEIVABLES" Foreground="{StaticResource TextMutedBrush}" FontSize="11" FontWeight="Bold" />
                        <TextBlock Text="{Binding Metrics.TotalReceivables, Converter={StaticResource CurrencyFormat}}" Foreground="#FBBF24" FontSize="18" FontWeight="Bold" Margin="0,6,0,2" />
                        <TextBlock Text="Outstanding Khata" Foreground="{StaticResource TextMutedBrush}" FontSize="11" />
                    </StackPanel>
                </Border>

                <!-- Payables -->
                <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}">
                    <StackPanel>
                        <TextBlock Text="SUPPLIER PAYABLES" Foreground="{StaticResource TextMutedBrush}" FontSize="11" FontWeight="Bold" />
                        <TextBlock Text="{Binding Metrics.TotalPayables, Converter={StaticResource CurrencyFormat}}" Foreground="#F87171" FontSize="18" FontWeight="Bold" Margin="0,6,0,2" />
                        <TextBlock Text="Pending Supplier Bills" Foreground="{StaticResource TextMutedBrush}" FontSize="11" />
                    </StackPanel>
                </Border>

                <!-- Cash Balance -->
                <Border Grid.Column="4" Style="{StaticResource CardBorderStyle}">
                    <StackPanel>
                        <TextBlock Text="CASH IN HAND" Foreground="{StaticResource TextMutedBrush}" FontSize="11" FontWeight="Bold" />
                        <TextBlock Text="{Binding Metrics.CashBalance, Converter={StaticResource CurrencyFormat}}" Foreground="White" FontSize="18" FontWeight="Bold" Margin="0,6,0,2" />
                        <TextBlock Text="Drawer / Vault Cash" Foreground="{StaticResource TextMutedBrush}" FontSize="11" />
                    </StackPanel>
                </Border>

                <!-- Bank Balance -->
                <Border Grid.Column="6" Style="{StaticResource CardBorderStyle}">
                    <StackPanel>
                        <TextBlock Text="BANK ACCOUNTS" Foreground="{StaticResource TextMutedBrush}" FontSize="11" FontWeight="Bold" />
                        <TextBlock Text="{Binding Metrics.BankBalance, Converter={StaticResource CurrencyFormat}}" Foreground="White" FontSize="18" FontWeight="Bold" Margin="0,6,0,2" />
                        <TextBlock Text="Commercial Accounts" Foreground="{StaticResource TextMutedBrush}" FontSize="11" />
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Recent Activity Table -->
            <Border Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Recent Sales &amp; Invoices" Foreground="White" FontSize="15" FontWeight="Bold" Margin="0,0,0,16" />

                    <DataGrid ItemsSource="{Binding Metrics.RecentActivities}" AutoGenerateColumns="False"
                              Background="Transparent" RowBackground="#131D31" AlternatingRowBackground="#1E293B"
                              BorderThickness="0" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#334155"
                              Foreground="White" HeadersVisibility="Column" IsReadOnly="True">
                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#0F172A" />
                                <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                                <Setter Property="Padding" Value="12,8" />
                                <Setter Property="BorderThickness" Value="0" />
                            </Style>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Padding" Value="12,10" />
                                <Setter Property="BorderThickness" Value="0" />
                            </Style>
                        </DataGrid.Resources>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="TYPE" Binding="{Binding ActivityType}" Width="100" />
                            <DataGridTextColumn Header="REFERENCE / INVOICE" Binding="{Binding Title}" Width="*" />
                            <DataGridTextColumn Header="AMOUNT (PKR)" Binding="{Binding Amount, Converter={StaticResource CurrencyFormat}}" Width="160" />
                            <DataGridTextColumn Header="STATUS" Binding="{Binding Status}" Width="160" />
                            <DataGridTextColumn Header="DATE / TIME" Binding="{Binding Timestamp, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Width="140" />
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
