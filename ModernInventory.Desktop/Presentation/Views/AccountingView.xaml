<UserControl x:Class="ModernInventory.Desktop.Presentation.Views.AccountingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:ModernInventory.Desktop.Presentation.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             Background="{StaticResource BgDarkBrush}">
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Accounting, Finance &amp; Vouchers" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,0,0,16" />

        <!-- Navigation Tabs Bar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <RadioButton Content="General Ledger" IsChecked="True" Style="{StaticResource ModernTabButtonStyle}" GroupName="AccTabs" Command="{Binding LoadDataCommand}" />
            <RadioButton Content="Vouchers" Style="{StaticResource ModernTabButtonStyle}" GroupName="AccTabs" />
            <RadioButton Content="Expenses &amp; Entry" Style="{StaticResource ModernTabButtonStyle}" GroupName="AccTabs" />
            <RadioButton Content="Chart of Accounts" Style="{StaticResource ModernTabButtonStyle}" GroupName="AccTabs" />
            <RadioButton Content="Monthly P&amp;L" Style="{StaticResource ModernTabButtonStyle}" GroupName="AccTabs" />
        </StackPanel>

        <!-- Main Content Area: General Ledger View -->
        <Border Grid.Row="2" Background="{StaticResource SurfaceDarkBrush}" BorderBrush="{StaticResource BorderDarkBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="General Ledger &amp; Double-Entry Audit Trail" Foreground="White" FontWeight="SemiBold" FontSize="15" Margin="0,0,0,12" />

                <DataGrid Grid.Row="1" ItemsSource="{Binding GeneralLedger}" AutoGenerateColumns="False" CanUserAddRows="False" 
                          Background="Transparent" BorderBrush="{StaticResource BorderDarkBrush}" RowBackground="{StaticResource SurfaceDarkBrush}"
                          AlternatingRowBackground="#1E293B" Foreground="White" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="{StaticResource BorderDarkBrush}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat={}{0:yyyy-MM-dd HH:mm}}" Width="150" />
                        <DataGridTextColumn Header="Voucher #" Binding="{Binding VoucherNumber}" Width="140" />
                        <DataGridTextColumn Header="Account Head" Binding="{Binding AccountName}" Width="200" />
                        <DataGridTextColumn Header="Description / Narration" Binding="{Binding Description}" Width="*" />
                        <DataGridTextColumn Header="Debit (₨)" Binding="{Binding Debit, StringFormat={}{0:N2}}" Width="110" />
                        <DataGridTextColumn Header="Credit (₨)" Binding="{Binding Credit, StringFormat={}{0:N2}}" Width="110" />
                        <DataGridTextColumn Header="Balance (₨)" Binding="{Binding RunningBalance, StringFormat={}{0:N2}}" Width="120" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
